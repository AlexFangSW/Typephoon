.container {
  width: 100%;
  min-height: var(--content-height);
  display: flex;
  flex-direction: column;
  align-items: center;
}

.token_container {
  display: flex;
  flex-direction: column;
  align-items: center;
  font-size: 1.5rem;
}

.token {
  display: flex;
  align-items: center;
  justify-content: center;
  column-gap: 1rem;
  padding-left: 1rem;
  border-radius: 0.5rem;
  border-style: solid;
  border-width: 2px;
}
