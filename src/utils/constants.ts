export enum CookieNames {
  ACCESS_TOKEN = "TP_AT",
  REFRESH_TOKEN = "TP_RT",
  USERNAME = "USERNAME",
}

export enum SessionStoreKeys {
  GAME_ID = "GAME_ID",
  GAME_STATISTICS = "GAME_STATISTICS",
}
